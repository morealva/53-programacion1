export default class Suma { 

    bi;
    bs;
    h;

    constructor() {}
   
    calcularSuma(){
        const resultado = (( parseInt(this.bi) + parseInt(this.bs)  ) /2)  * this.h ;
        return resultado;
    }
}

    
